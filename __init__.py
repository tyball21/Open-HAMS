from backend import app, db, ma, api

# Additional configurations or initializations (if any) can go here
# For instance, you might want to initialize logging, application-wide hooks, or other configurations

# Example: Initialize logging
import logging
logging.basicConfig(level=logging.INFO)
